<%= form_with(model: asignatura) do |form| %>
  <% if asignatura.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(asignatura.errors.count, "error") %> prohibited this asignatura from being saved:</h2>

      <ul>
        <% asignatura.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :tipo %>
    <%= form.text_field :tipo %>
  </div>

  <div class="field">
    <%= form.label :creditos %>
    <%= form.number_field :creditos %>
  </div>

  <div class="field">
    <%= form.label :grado_id %>
    <%= form.text_field :grado_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
