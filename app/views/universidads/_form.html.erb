<%= form_with(model: universidad) do |form| %>
  <% if universidad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(universidad.errors.count, "error") %> prohibited this universidad from being saved:</h2>

      <ul>
        <% universidad.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :tipo %>
    <%= form.select :tipo, [ 'Pública', 'Privada' ], :prompt => 'Seleccione una' %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :comunidad_id %>
    <%= form.select :comunidad_id, options_from_collection_for_select(Comunidad.all, :id, :nombre) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
