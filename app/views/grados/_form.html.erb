<%= form_with(model: grado) do |form| %>
  <% if grado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(grado.errors.count, "error") %> prohibited this grado from being saved:</h2>

      <ul>
        <% grado.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :universidad_id %>
    <%= form.text_field :universidad_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
